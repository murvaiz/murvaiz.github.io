
https://www.youtube.com/watch?v=PFmuCDHHpwk

Object oriented programming

-Encapsulation - properties and methods in an object.
group the things together, reduce complexity
let employee = {
	baseSalary: 30000,
	overtime: 10,
	rate: 20
	getWage: function() {
		return this.baseSalary + (this.overtime * this.rate);
	}
};

employee.getWage();
best functions with no parameter!

-Abstraction
hide the details
hide the methods from world

-Inheritance
eliminate the code duplication

-Polymorphism
different behavior
refactor statements

visual code
install live server

ES6
if the local variable name and the param name is the same it is enough to write only onec the name! :-) radius,

factory function
function createCircle(radius) ÿ{
return {
radius,
draw: function() { console.log(

createCircle(.....

constractor function
function Circle(radius) {
this.radius = radius;
this.draw = function() { .....

new Circle(...

without new the this is referencing the Window object

every object has a constructor   .constructor
object.length -- number of parameters

call - call a function with this and the input parameters
apply - call function with this and the parameters in an array

properties in javascript : Object.Properties (this, 'name', get: function(){}
